import { RezervacijaDestinacije } from './rezervacijaDestinacije';

describe('RezervacijaDestinacije', () => {
  it('should create an instance', () => {
    expect(new RezervacijaDestinacije()).toBeTruthy();
  });
});
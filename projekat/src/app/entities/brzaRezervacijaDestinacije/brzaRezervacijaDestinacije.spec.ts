import { BrzaRezervacijaDestinacije } from './brzaRezervacijaDestinacije';

describe('BrzaRezervacijaDestinacije', () => {
  it('should create an instance', () => {
    expect(new BrzaRezervacijaDestinacije()).toBeTruthy();
  });
});